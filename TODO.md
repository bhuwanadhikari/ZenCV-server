TODOS

- use simple llm or preprocessor to extract on jd from given text content - done
- use simple llm for detecting if it is really a job description page or not. - todo/currently uses open ai, use cheaper one instead
- use db instead of saving the file 
- create deepseek service for cheaper version 
- limit the char for the role.
- cover letter is too generic, make it interesting based on jd and cv 
- for cover letter, tell a custom lifestory 
- make the llm service usable with any of the llms, currently it is build for only open ai
- name product `AgileCV` - done
